-- ORACLE

SELECT *

FROM (

    SELECT NAME, DATETIME
    FROM ANIMAL_INS
    WHERE 1 = 1
    AND NOT EXISTS (
        SELECT 1
        FROM ANIMAL_OUTS
        WHERE 1 = 1
        AND ANIMAL_INS.ANIMAL_ID = ANIMAL_OUTS.ANIMAL_ID
    )
    ORDER BY 2 ASC

)

WHERE ROWNUM <= 3